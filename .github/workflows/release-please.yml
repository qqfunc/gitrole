name: Release Please

on:
  push:
    branches: [main]

jobs:
  release-please:
    name: Release Please
    runs-on: ubuntu-latest
    steps:
    - uses: googleapis/release-please-action@v5.0
      with:
        token: ${{ secrets.PAT_RELEASE_PLEASE }}
        release-type: python
